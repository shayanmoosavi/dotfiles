-- Declarative specification for layer rules
-- ==============================================================================================================================================

local blur = require("modules.rules.declarative.layer.blur")

local layer_specs = {
    Blur = blur
}

return layer_specs
