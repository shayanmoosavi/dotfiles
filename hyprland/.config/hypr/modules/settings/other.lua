-- Other Settings
-- ==============================================================================================================================================

-- Importing declarative specs
local decl_settings = "modules.settings.declarative"
local other = require(decl_settings .. ".other")

hl.config(other)
