# Animations
# ==============================================================================================================================================


animations {
    enabled = true
    workspace_wraparound = true

    animation = windowsIn, 1, 4, default, popin
    animation = windowsOut, 1, 4, default, popin

    animation = workspaces, 1, 7, default, slidefade 70%
    animation = layers, 1, 4, default, popin
}
